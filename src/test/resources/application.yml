spring:
  datasource:
    url: jdbc:tc:postgresql:9.6.12:///recognitions
  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.jdbc.core: TRACE
    org.springframework.data.mongodb: TRACE
